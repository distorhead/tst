name: main
on:
  pull_request:
    types: [edited]
  schedule:
  - cron:  '* * * * *'
jobs:
  myjob:
    name: myjob
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: mystep
      run: echo DONE
      shell: bash
